sources:
  - name: jaffle_shop
    description: A clone of a Postgres database.
    database: raw
    schema: jaffle_shop
    tables:
      - name: customers
        description: Raw customer data.
        columns:
          - name: id
            description: The primary key for raw customers.
            data_tests:
              - unique
              - not_null
      - name: orders
        description: Raw order data.